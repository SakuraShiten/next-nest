version: "1.1"

services:
  server:
    container_name: server
    build:
      context: ./server
      dockerfile: Dockerfile
    restart: always
    ports:
      - "5000:5000"
    networks:
      - my_network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.client-router.rule=Host(`coolify.io`) && PathPrefix(`/`)"
      - "traefik.http.routers.client-router.entryPoints=http"


networks:
  my_network:
    external: true