version: "1.1"

services:
  server:
    container_name: server
    build:
      context: ./server
      dockerfile: Dockerfile
    restart: always
    ports:
      - "5000:5000"
    networks:
      - my_network

networks:
  my_network:
    external: true